// Day 1
function gridSize() {
  
  var Y = GRID.length;
  var X = GRID[0].length;
  
  return (X.toString() + " x " + Y.toString());
}

// Day 2
function totalCells() {
  var Y = GRID.length;
  var X = GRID[0].length;
 
  return X*Y;  
}

